const {User} = require('./User');
const {VUserComplete} = require('./VUserComplete');
const {Ticket} = require('./Ticket');
const {VTicket} = require('./VTicket');
const {HistoryTicket } = require('./HistoryTicket');
const { Department } = require('./Department');
const { Role } = require('./Role');

module.exports = {
    User,
    VUserComplete,
    Ticket,
    HistoryTicket,
    Department,
    Role,
    VTicket
}